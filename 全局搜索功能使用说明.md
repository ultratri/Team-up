# 全局搜索功能使用说明

## 功能概述

全局搜索功能已经完全实现，可以快速搜索项目、用户和团队。

## 使用方法

### 1. 打开搜索框

有两种方式打开全局搜索：

- **点击顶部搜索栏**：点击页面顶部的搜索输入框
- **快捷键**：按 `Ctrl+K`（Windows/Linux）或 `Cmd+K`（Mac）

### 2. 搜索内容

在搜索框中输入关键词，系统会自动搜索：

- **项目**：根据项目标题和描述搜索
- **用户**：根据用户名和学号搜索
- **团队**：根据团队名称和描述搜索

### 3. 筛选结果

搜索结果支持分类筛选：

- **全部**：显示所有类型的搜索结果
- **项目**：只显示项目
- **用户**：只显示用户
- **团队**：只显示团队

### 4. 查看详情

点击任意搜索结果，会自动跳转到对应的详情页面：

- 项目 → 项目详情页
- 用户 → 用户个人主页
- 团队 → 团队详情页

### 5. 关闭搜索框

- 按 `ESC` 键
- 点击搜索框外的区域

## 技术实现

### 后端实现

- **接口地址**：`GET /api/search/global`
- **参数**：`keyword`（搜索关键词）
- **返回数据**：
  ```json
  {
    "projects": [...],
    "users": [...],
    "teams": [...],
    "totalCount": 10
  }
  ```

### 前端实现

- **组件位置**：`frontend/src/components/common/GlobalSearch.vue`
- **触发位置**：`frontend/src/layouts/Header.vue`
- **特性**：
  - 防抖搜索（300ms）
  - 快捷键支持（Ctrl+K）
  - 分类筛选
  - 实时搜索

## 搜索限制

- 每种类型最多返回 10 条结果
- 需要登录才能使用搜索功能
- 搜索关键词不能为空

## 后续优化建议

1. **搜索历史**：记录用户的搜索历史
2. **热门搜索**：显示热门搜索关键词
3. **高级搜索**：支持更多筛选条件（状态、时间范围等）
4. **搜索排序**：根据相关度、热度等排序
5. **全文搜索**：使用 Elasticsearch 提升搜索性能和准确度
