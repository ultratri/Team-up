# 综合广场（生态广场）完整开发文档

## 📋 项目概述

综合广场是一个统一的生态入口，整合了项目、人才、比赛、资源和动态等多个模块，为用户提供一站式的浏览和发现体验。

## ✅ 已完成的工作

### 1. 前端开发

#### 主页面
- **文件**: `frontend/src/views/ecosystem/EcosystemHub.vue`
- **功能**:
  - Hero区域展示（渐变背景、装饰光球）
  - 全局搜索框
  - 实时统计数据展示（活跃项目、在线人才、进行中比赛）
  - Tab导航切换（5个模块）
  - 响应式设计

#### 五个Tab模块

1. **项目大厅** (`ProjectHall.vue`)
   - 展示所有招募中和进行中的项目
   - 项目类型和状态筛选
   - 排序功能（最新、最热门、匹配度）
   - 新手友好标签、急需成员标签
   - 分页加载

2. **人才广场** (`TalentSquare.vue`)
   - 展示平台活跃用户
   - 院系和技能筛选
   - 新手/导师标识
   - 技能标签展示
   - 完成项目数和信誉分展示

3. **赛事中心** (`CompetitionCenter.vue`)
   - 展示所有比赛信息
   - 比赛级别和状态筛选
   - 即将截止提醒
   - 参赛队伍统计
   - 已集成真实API

4. **资源广场** (`ResourceSquare.vue`)
   - 展示项目成果、技术文档、面经分享等
   - 资源类型筛选
   - 排序功能（最新、浏览量、点赞数）
   - 点赞功能
   - **已集成真实API**

5. **动态广场** (`MomentSquare.vue`)
   - 展示用户活动动态
   - 动态类型筛选
   - 点赞功能
   - 时间格式化
   - **已集成真实API**

#### 路由配置
- **路径**: `/ecosystem`
- **名称**: `EcosystemHub`
- **权限**: 无需登录即可访问
- **导航**: 已添加到侧边栏菜单第一项

#### API接口
- **文件**: `frontend/src/api/ecosystem.ts`
- **功能**:
  - 统计数据API
  - 资源CRUD API
  - 动态CRUD API
  - 点赞/取消点赞API

### 2. 后端开发

#### 实体类（Entity）
- ✅ `Resource.java` - 资源实体
- ✅ `ResourceTag.java` - 资源标签关联
- ✅ `Moment.java` - 动态实体
- ✅ `Like.java` - 点赞实体

#### Mapper接口
- ✅ `ResourceMapper.java` - 资源数据访问
- ✅ `ResourceTagMapper.java` - 资源标签数据访问
- ✅ `MomentMapper.java` - 动态数据访问
- ✅ `LikeMapper.java` - 点赞数据访问

#### VO类（View Object）
- ✅ `ResourceVO.java` - 资源视图对象
- ✅ `MomentVO.java` - 动态视图对象
- ✅ `EcosystemStatsVO.java` - 统计数据视图对象

#### DTO类（Data Transfer Object）
- ✅ `ResourceCreateDTO.java` - 创建资源请求
- ✅ `MomentCreateDTO.java` - 创建动态请求

#### Service服务
- ✅ `ResourceService.java` - 资源服务接口
- ✅ `ResourceServiceImpl.java` - 资源服务实现
- ✅ `MomentService.java` - 动态服务接口
- ✅ `MomentServiceImpl.java` - 动态服务实现
- ✅ `EcosystemService.java` - 生态广场服务接口
- ✅ `EcosystemServiceImpl.java` - 生态广场服务实现

#### Controller控制器
- ✅ `EcosystemController.java` - 生态广场控制器
  - 统计数据接口
  - 资源CRUD接口
  - 动态CRUD接口
  - 点赞/取消点赞接口

### 3. 数据库

#### 数据库表
- ✅ `resources` - 资源表
- ✅ `resource_tags` - 资源标签关联表
- ✅ `moments` - 动态表
- ✅ `likes` - 点赞表
- ✅ `comments` - 评论表（预留）
- ✅ `views` - 浏览记录表（预留）

#### 迁移文件
- ✅ `V1.0.6__create_ecosystem_tables.sql`
- ✅ 已成功执行到数据库 `team_matching`

## 🎯 API接口文档

### 统计数据

#### 获取统计数据
```
GET /api/ecosystem/stats
```

**响应示例**:
```json
{
  "code": 200,
  "data": {
    "projectCount": 128,
    "talentCount": 456,
    "competitionCount": 12
  }
}
```

### 资源相关

#### 创建资源
```
POST /api/ecosystem/resources
```

**请求体**:
```json
{
  "title": "资源标题",
  "description": "资源描述",
  "type": "PROJECT",
  "cover": "封面图URL",
  "content": "资源内容（Markdown）",
  "projectId": 1,
  "tags": ["Vue.js", "Spring Boot"]
}
```

#### 获取资源列表
```
GET /api/ecosystem/resources?page=1&size=12&type=PROJECT&sortBy=latest
```

**参数**:
- `page`: 页码（默认1）
- `size`: 每页数量（默认12）
- `type`: 资源类型（可选：PROJECT/DOCUMENT/INTERVIEW/MATERIAL）
- `sortBy`: 排序方式（latest/views/likes）

#### 获取资源详情
```
GET /api/ecosystem/resources/{id}
```

#### 点赞资源
```
POST /api/ecosystem/resources/{id}/like
```

#### 取消点赞资源
```
DELETE /api/ecosystem/resources/{id}/like
```

#### 删除资源
```
DELETE /api/ecosystem/resources/{id}
```

### 动态相关

#### 创建动态
```
POST /api/ecosystem/moments
```

**请求体**:
```json
{
  "type": "CREATE_PROJECT",
  "content": "创建了一个新项目",
  "relatedProjectId": 1
}
```

#### 获取动态列表
```
GET /api/ecosystem/moments?page=1&size=10&type=CREATE_PROJECT
```

**参数**:
- `page`: 页码（默认1）
- `size`: 每页数量（默认10）
- `type`: 动态类型（可选：CREATE_PROJECT/JOIN_PROJECT/COMPLETE_PROJECT/GET_BADGE）

#### 点赞动态
```
POST /api/ecosystem/moments/{id}/like
```

#### 取消点赞动态
```
DELETE /api/ecosystem/moments/{id}/like
```

#### 删除动态
```
DELETE /api/ecosystem/moments/{id}
```

## 🗄️ 数据库表结构

### resources（资源表）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| title | VARCHAR(200) | 资源标题 |
| description | TEXT | 资源描述 |
| type | VARCHAR(50) | 资源类型 |
| cover | VARCHAR(500) | 封面图URL |
| content | LONGTEXT | 资源内容 |
| author_id | BIGINT | 作者ID |
| project_id | BIGINT | 关联项目ID |
| views | INT | 浏览量 |
| likes | INT | 点赞数 |
| created_at | TIMESTAMP | 创建时间 |
| updated_at | TIMESTAMP | 更新时间 |
| deleted | BOOLEAN | 逻辑删除 |

### resource_tags（资源标签表）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| resource_id | BIGINT | 资源ID |
| tag_name | VARCHAR(50) | 标签名称 |
| created_at | TIMESTAMP | 创建时间 |

### moments（动态表）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| user_id | BIGINT | 用户ID |
| type | VARCHAR(50) | 动态类型 |
| content | TEXT | 动态内容 |
| related_project_id | BIGINT | 相关项目ID |
| likes | INT | 点赞数 |
| comments | INT | 评论数 |
| created_at | TIMESTAMP | 创建时间 |
| deleted | BOOLEAN | 逻辑删除 |

### likes（点赞表）
| 字段 | 类型 | 说明 |
|------|------|------|
| id | BIGINT | 主键 |
| user_id | BIGINT | 用户ID |
| target_type | VARCHAR(50) | 目标类型 |
| target_id | BIGINT | 目标ID |
| created_at | TIMESTAMP | 创建时间 |

## 🚀 如何使用

### 前端访问
1. 启动前端开发服务器（已启动）
2. 访问：http://localhost:3001/ecosystem
3. 点击侧边栏"生态广场"菜单

### 后端启动
1. 确保数据库已创建表（已完成）
2. 启动后端服务：
```bash
cd backend/teamup-server
mvn spring-boot:run
```

### 测试API
使用Postman或curl测试API：
```bash
# 获取统计数据
curl http://localhost:8080/api/ecosystem/stats

# 获取资源列表
curl http://localhost:8080/api/ecosystem/resources?page=1&size=12

# 获取动态列表
curl http://localhost:8080/api/ecosystem/moments?page=1&size=10
```

## 📝 待完成功能

### 短期（1周内）
- [ ] 人才广场后端API（用户列表、筛选）
- [ ] 全局搜索功能
- [ ] 资源上传功能（文件上传）
- [ ] 评论功能实现

### 中期（2-4周）
- [ ] 智能匹配算法
- [ ] 推荐系统
- [ ] 浏览记录统计
- [ ] 热门资源排行

### 长期（1-2个月）
- [ ] 组队实验室
- [ ] 任务/悬赏广场
- [ ] 数据可视化
- [ ] 社交功能增强

## 🎨 设计特点

1. **统一的视觉风格**
   - 参考导师广场设计
   - 渐变背景（支持深色模式）
   - 装饰性光球效果
   - 卡片式布局

2. **用户体验优化**
   - 骨架屏加载
   - 空状态提示
   - 过渡动画
   - 响应式设计

3. **性能优化**
   - 分页加载
   - 懒加载
   - 防抖节流
   - 缓存策略

## 🔧 技术栈

### 前端
- Vue 3 + TypeScript
- Element Plus
- Vue Router
- Pinia
- SCSS

### 后端
- Spring Boot
- MyBatis-Plus
- MySQL
- Lombok

## 📊 项目结构

```
frontend/src/views/ecosystem/
├── EcosystemHub.vue          # 主页面
├── tabs/
│   ├── ProjectHall.vue       # 项目大厅
│   ├── TalentSquare.vue      # 人才广场
│   ├── CompetitionCenter.vue # 赛事中心
│   ├── ResourceSquare.vue    # 资源广场（已集成API）
│   └── MomentSquare.vue      # 动态广场（已集成API）
└── README.md

backend/teamup-server/src/main/java/com/teamup/server/modules/ecosystem/
├── controller/
│   └── EcosystemController.java
├── service/
│   ├── EcosystemService.java
│   ├── ResourceService.java
│   ├── MomentService.java
│   └── impl/
│       ├── EcosystemServiceImpl.java
│       ├── ResourceServiceImpl.java
│       └── MomentServiceImpl.java
├── entity/
│   ├── Resource.java
│   ├── ResourceTag.java
│   ├── Moment.java
│   └── Like.java
├── mapper/
│   ├── ResourceMapper.java
│   ├── ResourceTagMapper.java
│   ├── MomentMapper.java
│   └── LikeMapper.java
├── vo/
│   ├── ResourceVO.java
│   ├── MomentVO.java
│   └── EcosystemStatsVO.java
└── dto/
    ├── ResourceCreateDTO.java
    └── MomentCreateDTO.java
```

## 🎉 总结

综合广场的核心功能已经完成：

✅ **前端**：5个Tab模块全部实现，UI美观，交互流畅
✅ **后端**：完整的API接口，包括CRUD和点赞功能
✅ **数据库**：6张表已创建，索引优化完成
✅ **集成**：资源广场和动态广场已连接真实API
✅ **文档**：完整的开发文档和API文档

**下一步**：启动后端服务，测试完整功能流程！

---

**开发时间**：2026年2月10日
**开发者**：Kiro AI Assistant
**版本**：v1.0.0
