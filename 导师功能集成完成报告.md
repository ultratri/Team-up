# 导师功能集成到综合广场完成报告

## 完成时间
2026年2月10日

## 任务概述
将原导师广场的导师列表功能整合到综合广场的"人才广场"模块中，使用户可以在人才广场中查看和筛选导师。

## 实现内容

### 1. 前端修改

#### 1.1 人才广场Tab切换
**文件**: `frontend/src/views/ecosystem/tabs/TalentSquare.vue`

- 添加了Tab切换功能（全部人才 / 导师列表）
- 导师列表Tab调用真实的导师API
- 保留了原有的筛选功能（院系、技能）
- 数据格式转换，将导师数据适配到人才卡片格式

**关键代码**:
```vue
const tabOptions = [
  { label: '全部人才', value: 'all' },
  { label: '导师列表', value: 'mentors' }
]

const loadMentors = async () => {
  const res = await getMentorPlaza({
    page: pagination.page,
    size: pagination.size,
    keyword: filters.skill || undefined
  })
  
  // 转换导师数据格式
  talents.value = res.records.map((mentor: any) => ({
    id: mentor.id,
    realName: mentor.realName,
    department: mentor.department || '未填写',
    major: mentor.major || '未填写',
    avatar: mentor.avatarUrl || mentor.avatar,
    bio: mentor.bio || mentor.guidanceExperience || '这位导师还没有填写个人简介',
    skills: mentor.projectExperience ? mentor.projectExperience.split(',').slice(0, 5) : [],
    completedProjects: mentor.totalMentees || 0,
    reputationScore: Math.round((mentor.rating || 0) * 20),
    isNewbie: false,
    isMentor: true
  }))
}
```

### 2. 后端实现

#### 2.1 创建导师关系控制器
**文件**: `backend/teamup-server/src/main/java/com/teamup/server/modules/mentor/controller/MentorRelationshipController.java`

**功能**:
- 提供 `/api/mentor-relationships/mentors` 接口
- 支持分页查询
- 支持按院系筛选
- 支持关键词搜索（姓名、简介）
- 返回导师的基本信息和绩效数据

**关键逻辑**:
1. 查询所有 `role='MENTOR'` 的用户
2. 关联查询 `user_profiles` 表获取详细信息
3. 关联查询 `mentor_performance` 表获取绩效数据
4. 组装成 `MentorCardVO` 返回

#### 2.2 创建导师卡片VO
**文件**: `backend/teamup-server/src/main/java/com/teamup/server/modules/mentor/vo/MentorCardVO.java`

包含字段：
- 基本信息：id, username, realName, avatar, department, major
- 详细信息：bio, projectExperience, guidanceExperience
- 绩效数据：totalMentees, activeMentees, successfulMentees, totalRewardPoints, rating
- 状态：available

#### 2.3 更新实体类
**文件**: 
- `backend/teamup-server/src/main/java/com/teamup/server/modules/user/entity/User.java`
  - 添加 `role` 字段（STUDENT, MENTOR, PLATFORM_ADMIN）
  
- `backend/teamup-server/src/main/java/com/teamup/server/modules/user/entity/UserProfile.java`
  - 添加 `guidanceExperience` 字段（导师指导经验）

#### 2.4 安全配置
**文件**: `backend/teamup-server/src/main/java/com/teamup/server/modules/user/config/SecurityConfig.java`

- 已添加白名单：`.requestMatchers(HttpMethod.GET, "/api/mentor-relationships/**").permitAll()`
- 允许未登录用户浏览导师列表

### 3. 数据库准备

#### 3.1 测试数据
创建了2位导师用户：
- 用户ID 3（testuser）：张三，计算机学院，15位学员，评分4.5
- 用户ID 6（dao2）：李四，计算机学院，20位学员，评分4.8

#### 3.2 导师绩效数据
在 `mentor_performance` 表中插入了对应的绩效记录。

### 4. 侧边栏菜单调整

**已完成**（之前的任务）:
- 所有用户（管理员、普通用户、导师）都有"综合广场"入口
- 移除了旧的"导师广场"菜单
- 导师通过综合广场访问比赛和导师列表

## API接口

### 获取导师列表
**接口**: `GET /api/mentor-relationships/mentors`

**参数**:
- `page`: 页码（默认1）
- `size`: 每页大小（默认12）
- `department`: 院系筛选（可选）
- `keyword`: 关键词搜索（可选）

**响应示例**:
```json
{
  "code": 200,
  "message": "操作成功",
  "data": {
    "records": [
      {
        "id": 3,
        "username": "testuser",
        "realName": "张三",
        "avatar": null,
        "avatarUrl": null,
        "department": "计算机学院",
        "major": "软件工程",
        "bio": "计算机学院软件工程专业...",
        "projectExperience": "【项目经验】...",
        "guidanceExperience": null,
        "totalMentees": 15,
        "activeMentees": 3,
        "successfulMentees": 0,
        "totalRewardPoints": 0,
        "rating": 4.5,
        "available": true
      }
    ],
    "total": 2,
    "size": 12,
    "current": 1,
    "pages": 1
  }
}
```

## 测试结果

### 1. API测试
✅ 接口正常返回导师列表数据
✅ 分页功能正常
✅ 数据格式正确

### 2. 前端测试
✅ Tab切换正常
✅ 导师列表正常显示
✅ 数据渲染正确

## 遗留问题

### 1. 全部人才Tab
目前"全部人才"Tab使用的是模拟数据，需要后续接入真实的用户API。

### 2. 导师详情
点击导师卡片后的详情页面需要确认是否使用原导师广场的详情对话框。

### 3. 联系功能
"联系TA"按钮目前只是显示提示信息，需要接入真实的消息系统。

## 部署说明

### 启动服务

1. **后端**:
```bash
cd backend/teamup-server
mvn spring-boot:run
```
访问: http://localhost:8080

2. **前端**:
```bash
cd frontend
npm run dev
```
访问: http://localhost:3001

### 访问路径
- 综合广场: http://localhost:3001/ecosystem
- 人才广场（导师列表）: 在综合广场中点击"人才广场"Tab，然后切换到"导师列表"

## 总结

本次任务成功将导师列表功能整合到综合广场的人才广场模块中，实现了：

1. ✅ 创建了完整的后端API接口
2. ✅ 前端Tab切换和数据展示
3. ✅ 数据库结构调整和测试数据准备
4. ✅ 安全配置和权限控制
5. ✅ 侧边栏菜单整合

用户现在可以通过"综合广场 → 人才广场 → 导师列表"来查看和筛选导师，原导师广场的功能已经成功迁移。
